from copy import deepcopy


def main():
    # init first node

    # init node storage with first node

    # init working list with first node

    while True:
        pass
        # for loop over working list
            # create both nodes stemming from the og node

            # iterate over the one or two nodes
                # check if the nodes are in storage
                    # update the nodes in storage
                # else
                    # add the node into storage
                    # add to plan to work list

        # update working list


if __name__ == '__main__':
    main()
